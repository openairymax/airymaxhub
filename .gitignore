# =============================================================================
# OpenAirymax 伞仓 .gitignore
# 版本：18.0.0
# 最后更新：2026-07-10
# 策略：非必要不追踪 — 默认忽略所有，仅白名单伞仓必要文件
#
# 伞仓追踪清单：
#   .gitignore               — 本文件
#   .gitmodules              — 8 个 submodule 定义
#   README.md                — 项目说明
#   REPOSITORIES.md          — 仓库结构权威索引
#   .github/                 — GitHub Workflows / CI 配置
#   cmake/                   — 伞仓直属 CMake 模块（非 submodule）
#
# 各叶子仓自行维护：LICENSE, NOTICE, .editorconfig, .dockerignore,
#   AUTHORS.md, CHANGELOG.md, CONTRIBUTING.md, 语言 README 等
#
# 8 个 submodule（由 .gitmodules 定义）：
#   agentrt/  agentrt-linux/  sdk/  ecosystem/  products/
#   devtools/  docs/  docs-closed/
# =============================================================================

# ---------------------------------------------------------------------------
# 默认忽略所有
# ---------------------------------------------------------------------------
*

# ---------------------------------------------------------------------------
# 伞仓必要文件（白名单）
# ---------------------------------------------------------------------------
!.gitignore
!.gitmodules
!README.md
!REPOSITORIES.md

# ---------------------------------------------------------------------------
# 伞仓直属模块（非 submodule，需完整追踪）
# ---------------------------------------------------------------------------
!.github/
!cmake/
!cmake/**
