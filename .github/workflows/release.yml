name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 0.1.1)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  # 三平台并行构建：install 聚合到 stage 后打包
  # agentrt-<version>-<os>-<arch>.{tar.gz,zip}，结构对齐 install.sh 二进制
  # 模式（解压后 bin/ 直接可用）。制品命名规范：
  #   agentrt-<v版本>-<os>-<arch>（如 agentrt-v0.1.2-linux-x86_64.tar.gz）
  # 与 devtools/scripts/ci/release/package-full-release.sh 完全体包命名对齐，
  # 默认承载 atomgit，GitHub Release 为镜像。
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-x86_64
            pkg: tar.gz
          - os: macos-latest
            platform: macos-arm64
            pkg: tar.gz
          - os: windows-latest
            platform: windows-x64
            pkg: zip
    runs-on: ${{ matrix.os }}
    # 构建基线统一为 ubuntu:20.04（glibc 2.31），与二进制离线包一致：
    # 产物可覆盖 Ubuntu 20.04+/Debian 11+/Pi OS Bullseye+（2020 年后系统）。
    container: ubuntu:20.04
    env:
      BUILD_DIR: build
      STAGE_DIR: stage
    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      # 20.04 基线依赖准备：
      #   - apt: 构建工具 + 系统库（libcurl/libwebsockets/microhttpd 等）
      #   - pip: cmake ≥3.20（20.04 自带 3.16 不满足项目最低要求）
      #   - 源码: cJSON 1.7.18（20.04 的 1.7.10 缺 cJSON_GetNumberValue）
      #   - 源码: OpenSSL 3.0.17（20.04 的 libssl 1.1.1 头文件触发 AIRY
      #     poison 编译失败，且 EOL 不再维护）
      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential make perl curl git ca-certificates \
            python3 python3-pip python3-venv \
            libsqlite3-dev libyaml-dev libcurl4-openssl-dev \
            libssl-dev zlib1g-dev libzstd-dev \
            libmicrohttpd-dev libwebsockets-dev libevent-dev \
            libnghttp2-dev
          pip3 install --no-cache-dir cmake ninja
          curl -fsSL -o /tmp/cjson.tar.gz \
            https://github.com/DaveGamble/cJSON/archive/refs/tags/v1.7.18.tar.gz
          tar -xzf /tmp/cjson.tar.gz -C /tmp
          cmake -S /tmp/cJSON-1.7.18 -B /tmp/cjson-build \
            -DCMAKE_INSTALL_PREFIX=/usr/local \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DENABLE_CJSON_TEST=OFF -DBUILD_SHARED_LIBS=ON
          cmake --build /tmp/cjson-build --parallel
          cmake --install /tmp/cjson-build
          curl -fsSL -o /tmp/openssl.tar.gz \
            https://github.com/openssl/openssl/releases/download/openssl-3.0.17/openssl-3.0.17.tar.gz
          tar -xzf /tmp/openssl.tar.gz -C /tmp
          (cd /tmp/openssl-3.0.17 && ./config --prefix=/usr/local \
            --openssldir=/usr/local/ssl shared && \
            make -j"$(nproc)" && make install_sw)

      - name: Install build dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake ninja pkgconf sqlite cjson libyaml curl openssl zlib zstd \
            libmicrohttpd libwebsockets libevent nghttp2

      - name: Install build dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # vcpkg 包名：curl（非 libcurl）、libmicrohttpd/libwebsockets（带 lib- 前缀）
          vcpkg install sqlite3 cjson libyaml curl openssl zlib libmicrohttpd libwebsockets libevent nghttp2 --triplet x64-windows

      - name: Configure (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -S agentrt -B ${{ env.BUILD_DIR }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_TESTS=OFF \
            -DENABLE_SANITIZERS=OFF \
            -DAIRY_BUILD_ALL=ON \
            -DCMAKE_INSTALL_PREFIX="${{ env.STAGE_DIR }}"

      # openssl/zlib/sqlite 为 keg-only，需显式导出 pkg-config 路径供 find_package 使用
      - name: Configure (macOS)
        if: runner.os == 'macOS'
        run: |
          export PKG_CONFIG_PATH="$(brew --prefix openssl)/lib/pkgconfig:$(brew --prefix zlib)/lib/pkgconfig:$(brew --prefix sqlite)/lib/pkgconfig:$(brew --prefix curl)/lib/pkgconfig"
          cmake -S agentrt -B ${{ env.BUILD_DIR }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_TESTS=OFF \
            -DENABLE_SANITIZERS=OFF \
            -DAIRY_BUILD_ALL=ON \
            -DCMAKE_INSTALL_PREFIX="${{ env.STAGE_DIR }}"

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: |
          cmake -S agentrt -B ${{ env.BUILD_DIR }} `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" `
            -DCMAKE_BUILD_TYPE=Release `
            -DBUILD_TESTS=OFF `
            -DENABLE_SANITIZERS=OFF `
            -DAIRY_BUILD_ALL=ON `
            -DCMAKE_INSTALL_PREFIX="${{ env.STAGE_DIR }}"

      - name: Build
        run: |
          cmake --build ${{ env.BUILD_DIR }} --config Release --parallel

      - name: Install (aggregate bin/)
        run: |
          cmake --install ${{ env.BUILD_DIR }} --config Release

      # install.sh 二进制模式依赖 bin/agentrt-bootstrap.sh（daemon 编排脚本），
      # 但其位于 devtools 子仓（非 agentrt 树），cmake --install 不会带入，
      # 必须在此显式部署，否则二进制安装后 daemon 无法一键启动编排。
      - name: Deploy bootstrap script (tar.gz platforms)
        if: runner.os != 'Windows'
        run: |
          if [ -f "devtools/scripts/ops/bin/agentrt-bootstrap.sh" ]; then
            cp -f "devtools/scripts/ops/bin/agentrt-bootstrap.sh" "${{ env.STAGE_DIR }}/bin/agentrt-bootstrap.sh"
            chmod 755 "${{ env.STAGE_DIR }}/bin/agentrt-bootstrap.sh"
          fi

      # 打包：包内建 agentrt-<version> 顶层目录。install.sh/install.ps1 二进制
      # 模式按 agentrt-* 目录名匹配解压位置，无顶层目录时二进制安装 100% 失败
      # （回退源码构建 / 静默空装）。同时内置运行时配置模板供二进制模式
      # init_secrets 生成 secrets.env/agentrt.yaml/model.yaml。
      # 制品名统一为 agentrt-<v版本>-<os>-<arch>（如 agentrt-v0.1.2-linux-x86_64.tar.gz）。
      - name: Package (tar.gz)
        if: runner.os != 'Windows'
        run: |
          REF="${{ github.ref_name }}"; VERSION="${REF#v}"
          PKG_DIR="pkg/agentrt-${VERSION}"
          mkdir -p "${PKG_DIR}/config"
          cp -rf "${{ env.STAGE_DIR }}"/. "${PKG_DIR}/"
          cp -f ecosystem/manager/configs/agentrt.yaml "${PKG_DIR}/config/" 2>/dev/null || true
          cp -f ecosystem/manager/model/model.yaml "${PKG_DIR}/config/" 2>/dev/null || true
          cp -f devtools/scripts/ops/templates/secrets.env.example "${PKG_DIR}/config/" 2>/dev/null || true
          ARTIFACT="agentrt-${REF}-${{ matrix.platform }}"
          tar -czf "${ARTIFACT}.tar.gz" -C pkg .
          echo "artifact_name=${ARTIFACT}" >> "$GITHUB_ENV"

      - name: Package (zip)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $REF = "${{ github.ref_name }}"; $VERSION = $REF.TrimStart('v')
          $pkgDir = "pkg\agentrt-$VERSION"
          New-Item -ItemType Directory -Path $pkgDir -Force | Out-Null
          Copy-Item -Path "${{ env.STAGE_DIR }}\*" -Destination "$pkgDir\" -Recurse -Force
          $artifact = "agentrt-$REF-${{ matrix.platform }}"
          Compress-Archive -Path "pkg\*" -DestinationPath "$artifact.zip"
          "artifact_name=$artifact" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.artifact_name }}
          path: ${{ env.artifact_name }}.*
          if-no-files-found: error

  # RISC-V (linux-riscv64)：标准 runner 无 riscv64 第三方库（libcurl 等），
  # 经 Docker buildx + qemu 多架构容器构建（ubuntu:20.04 riscv64 镜像，
  # 与 x86_64/arm64 统一 glibc 2.31 基线）。
  build-riscv64:
    runs-on: ubuntu-latest
    env:
      BUILD_DIR: build
      STAGE_DIR: stage
    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Build in riscv64 container
        run: |
          docker run --rm --platform linux/riscv64 \
            -v "$PWD":/src -w /src ubuntu:20.04 bash -c '
            export DEBIAN_FRONTEND=noninteractive
            apt-get update -qq
            apt-get install -y -qq \
              build-essential make perl curl python3 python3-pip \
              libsqlite3-dev libyaml-dev libcurl4-openssl-dev \
              libssl-dev zlib1g-dev libzstd-dev libmicrohttpd-dev \
              libwebsockets-dev libevent-dev libnghttp2-dev
            pip3 install --no-cache-dir cmake ninja
            curl -fsSL -o /tmp/cjson.tar.gz \
              https://github.com/DaveGamble/cJSON/archive/refs/tags/v1.7.18.tar.gz
            tar -xzf /tmp/cjson.tar.gz -C /tmp
            cmake -S /tmp/cJSON-1.7.18 -B /tmp/cjson-build \
              -DCMAKE_INSTALL_PREFIX=/usr/local \
              -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
              -DENABLE_CJSON_TEST=OFF -DBUILD_SHARED_LIBS=ON
            cmake --build /tmp/cjson-build --parallel
            cmake --install /tmp/cjson-build
            curl -fsSL -o /tmp/openssl.tar.gz \
              https://github.com/openssl/openssl/releases/download/openssl-3.0.17/openssl-3.0.17.tar.gz
            tar -xzf /tmp/openssl.tar.gz -C /tmp
            (cd /tmp/openssl-3.0.17 && ./config --prefix=/usr/local \
              --openssldir=/usr/local/ssl shared && \
              make -j"$(nproc)" && make install_sw)
            cmake -S agentrt -B "$BUILD_DIR" -DCMAKE_BUILD_TYPE=Release \
              -DBUILD_TESTS=OFF -DENABLE_SANITIZERS=OFF -DAIRY_BUILD_ALL=ON \
              -DCMAKE_INSTALL_PREFIX="$PWD/$STAGE_DIR"
            cmake --build "$BUILD_DIR" -j"$(nproc)"
            cmake --install "$BUILD_DIR"
            '

      - name: Package (tar.gz)
        run: |
          REF="${{ github.ref_name }}"; VERSION="${REF#v}"
          PKG_DIR="pkg/agentrt-${VERSION}"
          mkdir -p "${PKG_DIR}/config"
          cp -rf "${{ env.STAGE_DIR }}"/. "${PKG_DIR}/" 2>/dev/null || true
          cp -f ecosystem/manager/configs/agentrt.yaml "${PKG_DIR}/config/" 2>/dev/null || true
          cp -f ecosystem/manager/model/model.yaml "${PKG_DIR}/config/" 2>/dev/null || true
          cp -f devtools/scripts/ops/templates/secrets.env.example "${PKG_DIR}/config/" 2>/dev/null || true
          cp -f devtools/scripts/ops/bin/agentrt-bootstrap.sh "${PKG_DIR}/bin/" 2>/dev/null || true
          ARTIFACT="agentrt-${REF}-linux-riscv64"
          tar -czf "${ARTIFACT}.tar.gz" -C pkg .
          echo "artifact_name=${ARTIFACT}" >> "$GITHUB_ENV"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agentrt-${{ github.ref_name }}-linux-riscv64
          path: agentrt-${{ github.ref_name }}-linux-riscv64.tar.gz
          if-no-files-found: error

  # 汇聚三平台制品到 GitHub Release
  release:
    needs: [build, build-riscv64]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: AgentRT ${{ github.ref_name }}
          body: |
            ## AgentRT ${{ github.ref_name }}

            AI Agent Runtime Platform Engineering — 服务器就绪的 Agent 运行时底座。

            ### 安装

            下载对应平台包并解压（或直接走安装器二进制模式）：

            ```sh
            # 以 Linux x86_64 为例；macOS → agentrt-<版本>-macos-arm64，Windows → .zip
            AIRY_RELEASE_URL="https://atomgit.com/openairymax/airymaxhub/releases/download/${{ github.ref_name }}/agentrt-${{ github.ref_name }}-linux-x86_64.tar.gz" ./install.sh
            ```

            或直接解压 tarball 后：
            - `bin/agentrt-bootstrap.sh start` 一键启动 17 个 daemon
            - `bin/airy_cli` 进入 CLI 交互界面（或 `airymaxrt`）
          files: dist/*
          draft: true
